import 'package:flutter/material.dart';

class ATSTemplateModern extends StatelessWidget {
  const ATSTemplateModern({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('ATS Template Modern')));
  }
}

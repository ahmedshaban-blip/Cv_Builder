import 'package:flutter/material.dart';

class ATSTemplateClassic extends StatelessWidget {
  const ATSTemplateClassic({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('ATS Template Classic')));
  }
}
